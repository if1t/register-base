import { InjectionToken } from '@angular/core';
import { IMenuStateService } from './register-table-filter.types';

export const MENU_STATE_SERVICE = new InjectionToken<IMenuStateService>('MENU_STATE_SERVICE');
